export const environment = {
  production: true,
  // TODO(Phase 10 — Backend Deploy): replace with the real hosted backend
  // URL once one exists; no production deployment exists yet.
  apiUrl: 'http://localhost:3000',
};
